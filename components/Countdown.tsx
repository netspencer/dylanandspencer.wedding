import React, { FunctionComponent } from 'react'
import classNames from 'classnames'

const Countdown: FunctionComponent = () => (
  <section className={classNames('p-8')}>88 days until wedding</section>
)

export default Countdown
